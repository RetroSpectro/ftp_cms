doctype html
html(lang='en')
  include common/head.pug
  +head("CMS FTP")
  body
    header
    include common/navbar.pug 
    +navbar(user)
    .container
      h1= title
      p Welcome to #{title}      
      if user 
        h3 You enter as  #{user.role}
  include common/footer.pug
  +footer()